# Copy to .env and fill in your keys: cp env.example .env
# .env is gitignored; direnv auto-loads it (see .envrc), or pass --env-file .env to uv run.

# Scraper (src/parser.py) — https://firecrawl.dev
FIRECRAWL_API_KEY=

# Researcher (src/research.py) — https://parallel.ai
PARALLEL_API_KEY=
